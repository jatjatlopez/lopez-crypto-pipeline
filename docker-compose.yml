version: "3.8"

services:
  ingestion:
    build: .
    container_name: crypto-ingestion
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./ingestion/state.json:/app/ingestion/state.json
